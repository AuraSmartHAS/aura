import 'package:get_it/get_it.dart';

void setupProfileModule(GetIt sl) {
  // Profile feature doesn't have any domain logic or BLoCs for now
  // It's a simple UI feature that displays information
}
