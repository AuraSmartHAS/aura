<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
    <relativePath/>
  </parent>

  <groupId>br.com.fiap.aura</groupId>
  <artifactId>aura-api</artifactId>
  <version>1.0.0</version>
  <name>AURA Care-Chain API</name>
  <description>Backend Spring Boot do AURA Care-Chain (FIAP - Smart HAS, Fase 5)</description>

  <properties>
    <java.version>21</java.version>
    <test.groups></test.groups>
    <test.excludedGroups>postgres</test.excludedGroups>
    <!-- Acima do que o Boot 3.3.x gerencia: a versao antiga negocia API 1.32 do Docker e o
         Docker Desktop 29 exige 1.40 no minimo, entao o container nem chega a subir. -->
    <testcontainers.version>1.20.4</testcontainers.version>
    <jjwt.version>0.12.6</jjwt.version>
    <springdoc.version>2.6.0</springdoc.version>
    <firebase-admin.version>9.4.3</firebase-admin.version>
    <!-- Casada com o google-http-client que o firebase-admin 9.4.3 resolve (1.45.3). -->
    <google-http-client.version>1.45.3</google-http-client.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <!--
      Push (C2). Nenhum BOM do Spring Boot gerencia o firebase-admin: a versão vai pinada à mão e
      ela arrasta grpc, guava e protobuf. As exclusões tiram Firestore e Cloud Storage, que o FCM
      não usa — sem elas entram ~90 jars a mais.
      ⚠️ Restrição descoberta na marra: o google-http-client-jackson2 chegava por baixo do
      Firestore, e o FirebaseOptions o carrega no construtor. Sem a linha explícita abaixo, o
      caminho COM credencial estoura NoClassDefFoundError — e só em produção, porque sem credencial
      o SDK nunca é instanciado. É o que o FirebaseConfigTest passou a cobrir.
    -->
    <dependency>
      <groupId>com.google.firebase</groupId>
      <artifactId>firebase-admin</artifactId>
      <version>${firebase-admin.version}</version>
      <exclusions>
        <exclusion>
          <groupId>com.google.cloud</groupId>
          <artifactId>google-cloud-firestore</artifactId>
        </exclusion>
        <exclusion>
          <groupId>com.google.cloud</groupId>
          <artifactId>google-cloud-storage</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>com.google.http-client</groupId>
      <artifactId>google-http-client-jackson2</artifactId>
      <version>${google-http-client.version}</version>
    </dependency>

    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>runtime</scope>
    </dependency>
    <!-- Schema versionado no perfil postgres. O modulo do PostgreSQL vem separado no Flyway 10.x,
         que e a versao gerenciada pelo Boot 3.3.x; sem ele a migracao nao reconhece o banco. -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Um unico teste sobe PostgreSQL de verdade (PostgresSchemaSmokeTest). A suite rapida
         continua em H2; isto existe para o schema do ensaio nao ficar sem nenhuma cobertura. -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- A suite padrao continua em H2 e sem Docker. O teste marcado com `postgres` sobe um
               container de verdade e roda sob demanda:
                 ./mvnw test -Dtest.groups=postgres -Dtest.excludedGroups=
               Propriedade, e nao valor fixo: com valor fixo o -D da linha de comando nao
               sobrescreve e o teste do Postgres nunca chega a rodar. -->
          <groups>${test.groups}</groups>
          <excludedGroups>${test.excludedGroups}</excludedGroups>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
